doctype html

block vars

html(lang = "ja")
    head
        include props/head.pug

        title #{title}
    body
        block body

        hr
        p: a(href = "../index.html") TOPに戻る

        script(src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js")

